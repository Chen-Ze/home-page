import { sharedDataAccessDataAccessSongGame } from './shared-data-access-data-access-song-game';

describe('sharedDataAccessDataAccessSongGame', () => {
  it('should work', () => {
    expect(sharedDataAccessDataAccessSongGame()).toEqual(
      'shared-data-access-data-access-song-game'
    );
  });
});
